import BookList from '@/components/BookList';

const MasterBookPage = () => {
  return (
    <div className="min-h-screen bg-gray-100 flex items-center justify-center p-6">
      <BookList />
    </div>
  );
};

export default MasterBookPage;
